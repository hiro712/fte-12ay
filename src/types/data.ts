export type FlightData = {
  time: string;
  mode: number;
  pin: number;
  height: number;
  latitude: number;
  longitude: number;
};

export type ProjectData = {
  name: string;
  contents: string;
  isApploading: boolean;
};
